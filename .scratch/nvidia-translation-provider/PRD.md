# NVIDIA NIM translation provider

Add NVIDIA's hosted NIM API as a translation provider without requiring an
NVIDIA SDK. Use the OpenAI-compatible API, discover currently available text
models, and make the dedicated Riva translation model the safe default.

