# Galaxy web migration

Galaxy Studio migrates from tkinter to a pywebview desktop shell backed by
FastAPI and a React frontend. The service layer and command-line workflows
remain independent of either UI.

The detailed phase plan lives at
`tools/galaxy_ai_voice_subtitle_studio/docs/web-migration-plan.md`.
